#include<stdio.h>
void main(){

    int a ;
    printf("enter a integer number:");
    scanf("%d",&a);
    if (a==0)
    printf("a is zero");
    if (a<0)
    printf("a is negative");
    if (a>0)
    printf("a is positive");

}
